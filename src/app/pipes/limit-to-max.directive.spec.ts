import {LimitToMaxDirective} from './limit-to-max.directive';

describe('LimitToMaxDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitToMaxDirective();
    expect(directive).toBeTruthy();
  });
});
